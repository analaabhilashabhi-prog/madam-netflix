Drop your soft background track here as: soft.mp3

It is used automatically by the photo viewer (Section 4.11). Until this file
exists, the app plays a gentle generated piano/pad loop instead, so the
"music ducks while she reads" behaviour still works.

To point at a different filename or a URL, edit PHOTO_MUSIC in src/config.js.
